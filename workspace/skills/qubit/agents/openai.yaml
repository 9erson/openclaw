interface:
  display_name: "Qubit"
  short_description: "Run OpenClaw pillar workflows in Discord"
  default_prompt: "Use $qubit to run pillar workflows with lazy-loaded references/templates and deterministic script primitives."
